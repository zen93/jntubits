<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white" >
    <TextView
        android:id="@+id/mcqLearnCorrectTV"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <TextView android:text="With suitable arithmetic laws and weights, path expressions are converted into algebraic functions that are called?"
        android:id="@+id/mcqLearnQuestionTextView"
        android:textAppearance="@style/QuestionText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ListView
        android:id="@+id/mcqLearnAnswerChoicesList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:choiceMode="singleChoice"
        android:layout_alignParentLeft="true"
        android:divider="@color/divider_color"
        android:dividerHeight="1dp" >
    </ListView>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/mcqLearnSubmitButton"
        android:text="Next"
        />

</LinearLayout>