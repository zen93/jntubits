<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@android:color/white"
    tools:context="in.momofactory.jntubits.MCQRevFragment">

    <TextView android:text="With suitable arithmetic laws and weights, path expressions are converted into algebraic functions that are called?"
        android:id="@+id/mcqRevQuestionTextView"
        android:textAppearance="@style/QuestionText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ListView
        android:id="@+id/mcqRevAnswerChoicesList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:listSelector="@android:color/transparent"
        android:choiceMode="singleChoice"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/mcqRevQuestionTextView"
        android:divider="@color/divider_color"
        android:dividerHeight="1dp" >
    </ListView>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/mcqRevSubmitButton"
        android:layout_below="@+id/mcqRevAnswerChoicesList"
        android:text="Next"
        />

</RelativeLayout>
